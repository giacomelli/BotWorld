 <Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
 
 <Target Name="AfterBuild">
   	<ItemGroup>
    	<FilesToCopy Include="$(TargetPath)" />
	</ItemGroup>

    <Copy
        SourceFiles="@(FilesToCopy)"
        DestinationFolder="..\DG.BotWorld.World.MvcApp\bin\$(Configuration)\World\Sources\Bots\$(Bot)\Abilities"
    />
    <Copy
        SourceFiles="@(FilesToCopy)"
        DestinationFolder="..\DG.BotWorld.WorldMatrix.WinApp\bin\$(Configuration)\World\Sources\Bots\$(Bot)\Abilities"
    />
</Target>

</Project>